version: 1
frontend:
  phases:
    build:
      commands: []   # Sin comandos de build
  artifacts:
    baseDirectory: / # Busca archivos en la raíz
    files:
      - '**/*'       # Incluye TODOS los archivos
  cache:
    paths: []        # No usa caché